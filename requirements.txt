numpy>=1.20.0
scipy>=1.7.0
scikit-image>=0.18.0

# Optional GPU acceleration dependencies (Phase 4.1)
# Install based on your hardware:
# - For NVIDIA GPUs: pip install cupy-cuda11x (or cupy-cuda12x)
# - For Apple Silicon: Metal is built-in, no extra dependencies
# - For OpenCL: pip install pyopencl

# Optional REST API dependencies (Phase 4.3)
# Install for API functionality:
# pip install "fastapi[all]" uvicorn

# Property-based testing dependencies (Phase 4.4)
# Install for advanced testing:
# pip install hypothesis
